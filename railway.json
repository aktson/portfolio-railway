{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "nix-env -iA nixpkgs.nodejs-${NODE_VERSION} && nix-env -iA nixpkgs.python${PYTHON_VERSION} && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "nix-env -iA nixpkgs.python${PYTHON_VERSION} && npm start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "never"
  }
}
