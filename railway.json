{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "nix-env -iA nixpkgs.nodejs-14 && nix-env -iA nixpkgs.python3.10 && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "pyenv install 3.10.11 && pyenv global 3.10.11 && nix-env -if .nixpacks/nixpkgs-bf744fe90419885eefced41b3e5ae442d732712d.nix && nix-collect-garbage -d",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "never"
  }
}
